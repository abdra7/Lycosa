# Lycosa Dashboard

Native Flutter Desktop operator dashboard (macOS/Windows/Linux): node
inventory, live metrics, workflow runs, knowledge collections, logs and
alerts. Distributed as per-OS installers via GitHub Releases, not as a
Docker service (see ADR-006).

Implemented in **Sprint 8**. Placeholder for now.
