/// App version shown in the UI. Keep in sync with pubspec.yaml `version:`
/// (release builds also pass it via --build-name; this constant is the
/// single in-app source so widgets don't need an async package_info lookup).
const String appVersion = '0.1.0';
