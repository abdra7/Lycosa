# Placeholder Prometheus config for Sprint 0.
# Real scrape targets (api /metrics, per-node exporters) land in Sprint 9.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
